<Window x:Class="EliteMFD.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="EliteMFD 0.5-beta" Height="350" Width="525" WindowStartupLocation="Manual">

    <Border Padding="5">
        <TabControl Name="TabControl" ItemsSource="{Binding}" SelectionChanged="TabControl_OnSelectionChanged">
            <TabControl.Resources>
                <DataTemplate x:Key="TabHeader" DataType="TabItem">
                    <DockPanel>
                        <Button Name="DeleteButton" DockPanel.Dock="Right" Margin="5,0,0,0" Padding="0"
                                Click="DeleteButton_OnClick" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type TabItem}}, Path=Name}"
                                Content="X" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                        <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type TabItem}}, Path=Header}"/>
                    </DockPanel>
                </DataTemplate>
            </TabControl.Resources>
        </TabControl>
    </Border>
</Window>

